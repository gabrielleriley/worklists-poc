export interface ICreateEntityStoreSchema {
    pagedLibraryPath: string;
    statusLibraryPath: string;
    directory: string;
    name: string;
    paginated: boolean;
    queryParams: boolean;
}